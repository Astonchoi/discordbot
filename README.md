# Discord Meme Bot

## Prerequisites

1. Get a Discord application
2. Create an application and get the token
3. Allow send message permission
4. Create an OAuth link for giving the bot permission and inviting it to the server

## Steps to Run the Application

1. Create a .env file using the command:
   ```bash
   cp .env.example .env
   ```
2. Change the token to your application token
3. Install all the required libraries using the command:
   ```bash
   pip install -r requirements.txt
   ```
4. Start the application using the command:
   ```bash
   python Memebot.py
   ```
